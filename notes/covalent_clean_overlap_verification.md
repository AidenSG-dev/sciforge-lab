Final clean-overlap verification:

The paused Cl2 viewport now shows compact inner shells and a larger outer valence shell. The inner shells remain separate when the atoms approach. Only the outer valence shells intersect in the final stable state. The shared pair is represented by two solid yellow dots in the overlap lens. Non-bonding electrons remain white on their shell circumferences. No decorative bond lines or yellow glow particles are rendered.

Checks passed: TypeScript and production build. Browser route reached READY, Play reached SHELLS OVERLAP, and the final state reached STABLE MOLECULE.
